# ba
## git config --global --list --> revisar cuenta
### git config --global user.name "nombre_usuario" --> cambiar o registrar usuario
### git config --global user.email "bastian.nunez27@inacapmail.cl" --> cambiar o registrar correo
### git add .
### git commit -m "***"
### git push origin main
### pip freeze > requirements.txt --> antes de hacer git add
### pip install django (con venv activado)
### django-admin startproject/app "elnombre" . (comenzar projecto y app)
### python manage.py runserver (abrir la pagina)
### git branch ramainicio (crear rama)
### git checkout ramainicio (cambiar rama)